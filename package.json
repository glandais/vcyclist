{
  "private": true,
  "name": "vcyclist-release-tools",
  "description": "Dev-time tooling for cutting vcyclist releases (semantic-release). Not published.",
  "scripts": {
    "release": "semantic-release"
  },
  "devDependencies": {
    "semantic-release": "^25.0.9",
    "@semantic-release/changelog": "^7.0.0",
    "@semantic-release/exec": "^7.0.3",
    "@semantic-release/git": "^11.0.1"
  }
}
